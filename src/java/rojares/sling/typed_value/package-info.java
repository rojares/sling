/**
 * This package contains DValue which is the interface all values from David implement and DType the enumerates
 * all those types. The package is called typed_value because the classes here and in subpackeges implement values that
 * have belong to those types that David supports. Type is a set of values.
 */
package rojares.sling.typed_value;